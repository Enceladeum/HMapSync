# ── Build output ──
bin/
obj/

# ── IDE / editor ──
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# ── Dalamud packager / release artifacts ──
*.zip
# ...but DO track the published distribution zip at repo root (tagless self-hosted feed)
!/latest.zip

# ── OS cruft ──
Thumbs.db
.DS_Store
